{
  "name": "evilhamster",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/worker.js",
    "set-webhook": "node scripts/set-webhook.js"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
